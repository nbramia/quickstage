# API Configuration
# Prefer leaving this unset; the app will default to `/api` which works
# with the Vite dev proxy and Cloudflare Pages → Worker routing in prod.
# Uncomment to override.
# VITE_API_BASE_URL=https://quickstage.tech/api

# Google OAuth
# Create an OAuth 2.0 Client ID (type: Web application) and set:
# - Authorized JavaScript origins:
#   - http://localhost:3000 (dev via Vite)
#   - https://quickstage.tech (prod)
# - Authorized redirect URIs:
#   - http://localhost:3000 (token-based flow used; no path needed)
#   - https://quickstage.tech
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id

# Optional: Development overrides
# VITE_API_BASE_URL=http://localhost:8787/api
